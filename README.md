# AS Survey GitHub Pages

This branch serves the static prototype UI through GitHub Pages.

Important: GitHub Pages serves static HTML/CSS/JS only. The FastAPI backend and SQLite database do not run on GitHub Pages. For real login/survey submission, set the API Base URL in the UI Settings page to a hosted backend URL.
